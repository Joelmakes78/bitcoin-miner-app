# Core dependencies
PyQt6>=6.4.0
requests>=2.28.0
numpy>=1.24.0
pandas>=2.0.0
cryptography>=40.0.0

# GPU acceleration (optional - install based on your hardware)
# For AMD/Intel GPUs:
pyopencl>=2023.1

# For NVIDIA GPUs:
# pycuda>=2022.1

# Performance optimizations
orjson>=3.9.0      # Faster JSON parsing
uvloop>=0.17.0     # Faster async event loop (Linux/macOS only)